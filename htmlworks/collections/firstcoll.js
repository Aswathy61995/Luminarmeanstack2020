

//array
//in javascriot there is no size limit for array
//we can access each element by using array index
var name="mumbai"
var names=["mumbai","delhi"]
console.log(names[0])
names.push("csk")
console.log(names)
names.pop()
console.log(names)
names.push(1500);// we can store different type of objects
console.log(names);
var numbers=[10,11,12,13];
console.log(numbers[0])
for (elements of numbers){
    console.log(elements)
}